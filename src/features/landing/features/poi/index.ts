import { createComingSoonLandingFeaturePresentation } from "@/features/landing/features/shared";

export const poiPresentation = createComingSoonLandingFeaturePresentation(
  "poi",
  "Sélection et édition des points d’intérêts (POI)",
);