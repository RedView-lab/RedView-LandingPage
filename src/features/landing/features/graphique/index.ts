import { createComingSoonLandingFeaturePresentation } from "@/features/landing/features/shared";

export const graphiquePresentation = createComingSoonLandingFeaturePresentation(
  "graphique",
  "Graphique customisable",
);