import { createComingSoonLandingFeaturePresentation } from "@/features/landing/features/shared";

export const comparaisonPresentation = createComingSoonLandingFeaturePresentation(
  "comparaison",
  "Comparaison d’itinéraire",
);