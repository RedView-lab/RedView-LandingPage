import { createComingSoonLandingFeaturePresentation } from "@/features/landing/features/shared";

export const timelinePresentation = createComingSoonLandingFeaturePresentation(
  "timeline",
  "Feuille de route et timeline exportables",
);